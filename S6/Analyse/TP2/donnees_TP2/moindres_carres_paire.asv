function [parametres] = moindres_carres_paire(d,x,y1,y2,beta_0,gamma_0)

    p = length(x);
    
    % On construit les matrices A et B
    A = zeros(2*p, 2*d - 1);
    for k = 1:d-1
        A(1:p, k) = nchoosek(d, k) * x.^k .* (1-x).^(d-k);
    end

    for k = d:2*d-1
        A(p+1:end, k) = nchoosek(d, k) * x.^k .* (1-x).^(d-k);
    end

    A(:, 2*d - 1) = 

    B = y - (beta_0 * (1-x).^d);

    beta = A\B; % beta = (A'A)−1 A' B

end

